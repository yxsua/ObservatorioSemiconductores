services:

  nginx:

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - ./docker/nginx/production.conf:/etc/nginx/conf.d/default.conf:ro

  frontend:

    restart: unless-stopped

  backend:

    restart: unless-stopped

  postgres:

    restart: always